# BiologyAdvisor · 2026-09-14 增量计划

**对应主计划**:项目开发计划.md §6 Phase 1 第 1 项前置工程化骨架
**今日项**:Phase 1 ETL 启动期 — 落地 dry-run 工具,零依赖静态解析 etl_schema.sql,延续 9-11 DDL + 9-12 README 推进信号

**涉及文件**:
- 新建 `scripts/etl_dryrun.py`(stdlib only · 正则解析 CREATE TABLE / CREATE INDEX · 输出 3 表 + 字段数 + 索引数 · 无网络 / 无 DB 连接)

**验收**:
- 脚本退出码 0,输出汇总报告(3 表 / 字段数 / 索引数)
- 无 §5/§6 checkbox 变更(本次属于"工程化骨架准备",不构成 §6 任一阶段产物完成;沿用 9-11 cron T4 模式)
- git commit 记录在案

**不做**:
- 不连 PostgreSQL(9-12 README §2 已给应用方法,本节点不重复)
- 不引入 psycopg2 / SQLAlchemy(留 Phase 1 实施期决策,见 9-11 commit message)
- 不写真 ETL 拉取逻辑(留 §6 第 1 项,9-15 节点起)
