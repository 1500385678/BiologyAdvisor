# BiologyAdvisor · 2026-09-05 增量计划

**对应主计划**:项目开发计划.md §5 Phase 0 第 4 项
**节点**:**Phase 0 收尾日** · API Key 申请 + 配置骨架落库
**触发**:2026-09-04 cron T4 预备清单已就位(见 `docs/architecture/api-keys-checklist.md`)

## 今日 1 个小变更(简洁可入库)

把预备清单 §3.2 / §3.3 / §2.4 中"可直接落库、不依赖人工账号"的部分实体化:

### 涉及文件
- **新建** `BiologyWeb/config/clinicaltrials.yaml` — 临床试验 API 配置骨架(用户代理 / 速率限制 / 礼貌爬取)
- **新建** `BiologyWeb/scripts/verify_clinicaltrials.py` — 验证 ClinicalTrials.gov 可达性(无需 Key,09-05 节点真跑)
- **新建** `BiologyWeb/scripts/verify_ncbi_key.py` — 验证 NCBI API Key 是否生效(需 Key 落环境变量,人工)
- **更新** `.gitignore` — 显式加 `.env`(预备清单 §2.5 写"已在 .gitignore 显式排除",实际只有 `*` 通配,需补)
- **更新** `项目开发计划.md` §5 #4 checkbox 文字 — 拆为"配置骨架已落库" + "NCBI Key 真申请留人工"两段状态

### 不做什么
- **不**真去申请 NCBI API Key(需人工登录 NCBI 账号、点击"Create API Key"按钮,agent 无浏览器/账号态)
- **不**勾 §5 checkbox 的 `- [x]`(回填指引 §7 明示:"没拿到 Key 的情况下不要提前勾选,影响进度可信度")
- **不**新建 ETL 流水线(那是 Phase 1 MVP,不是 Phase 0 收尾)
- **不**改 Neo4j schema(已完成,见 `docs/architecture/neo4j-schema-v1.md`)
- **不**引入新依赖(`requests`/`pyyaml` 检查在 requirements.txt 是否已含,缺则补,优先直接用 stdlib + 现成依赖)

## 验收
- `config/clinicaltrials.yaml` 存在,含 base_url / user_agent / rate_limit / politeness 4 段
- 2 个 verify_*.py 脚本存在,语法合法(`python3 -m py_compile` 通过)
- `verify_clinicaltrials.py` 实跑成功,输出 `✅ ClinicalTrials.gov 可达,EGFR 查询返回 N 条`
- `.gitignore` 显式含 `.env` 与 `*.key`
- git commit 信息形如 `chore: 09-05 节点 Phase 0 #4 配置骨架 + 验证脚本`
- gitee / github push 成功(github 允许 30s 超时失败)
