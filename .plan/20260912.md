# BiologyAdvisor · 2026-09-12 增量计划

**对应主计划**:项目开发计划.md §5 Phase 0 §6 Phase 1 第 1 项(5 ETL 流水线 · DDL 子项)
**今日项**:为 9-12 T4 cron 刚落地的 `app/db/etl_schema.sql` 写配套 README,说明如何应用 DDL + 连接串模板 + 迁移工具链占位说明

**背景**:
- `e10e779`(9-12 T4 cron 提交)落地 `app/db/etl_schema.sql`(etl_state 11 列 + etl_run_log + etl_quarantine 三表 + 索引/外键/CHECK 约束)+ `app/db/__init__.py` 子模块占位
- 这是 §6 第 1 项首个非文档类可入库代码,从"前置设计 100% + 启动 0%"推到"DDL 100% + 实跑 0%"
- 但裸 SQL 文件没有 README 说明如何应用 + 如何回滚 + 连接串怎么配,Phase 1 ETL 启动前必须补上
- 9-12 巡检已第 6 次建议"写一份 app/db/README.md 说明如何应用 DDL"

**涉及文件**:
- 新建 `/Users/aaron/Mac/Consultant/17-生物-Biology/_BiologyLib/BiologyWeb/app/db/README.md`
  - §1 文件清单(etl_schema.sql + `__init__.py` 各自作用)
  - §2 快速应用(psql -f 命令 + DATABASE_URL 环境变量模板)
  - §3 三表说明(etl_state / etl_run_log / etl_quarantine 一句话定义)
  - §4 验证查询(SELECT 1 FROM etl_state LIMIT 0 等 4 条 smoke test)
  - §5 迁移工具链(alembic 暂未引入,Phase 1 启动前再决议,占位段)
  - §6 不做什么(不写 ORM 封装 / 不写连接池 / 不写 DDL 自动化)

**验收**:
- `app/db/README.md` 存在,含 §1-§6 全部小节
- 包含至少 1 条 psql 应用命令可直接复制
- 不引入新依赖(纯文档)
- git commit 记录在案
- 不修改 `项目开发计划.md`(本工作不直接对应任何 open checkbox,§6 第 1 项仍属"DDL 已落地 + 实跑 0%")

**与今日其他 P0 关系**:
- 主人动作(API Key 真申请):不阻塞本任务
- LLM 盲测 / dump 末节点 / 收尾报告:与本任务可独立并行,不在本 cron 窗口内处理
- etl_schema.sql 迁移工具链(alembic 引入决议):本 README §5 留占位段,**不**实际引入 alembic(等 Phase 1 启动决议)
